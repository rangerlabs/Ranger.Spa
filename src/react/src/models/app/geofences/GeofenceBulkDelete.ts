import { ShapePicker } from '../../../redux/actions/GoogleMapsActions';
import CorrelationModel from '../../CorrelationModel';
import Schedule from '../../Schedule';

export default interface GeofenceBulkDelete {
    externalIds: string[];
}
